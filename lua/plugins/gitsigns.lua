return {
  'lewis6991/gitsigns.nvim',
  config = function()
    require('gitsigns').setup {
      -- Add any custom gitsigns configuration here
      -- signs = { ... },
      -- numhl = false,
      -- ... other options
    }
  end
}
